============
 ISTRUZIONI
============
ATTENZIONE: Questa cartella contiene il necessario per generare la versione indicata e basta!
nella cartella "files" mettere tutti i files che devono valere per questa versione e che nel
progetto generale potrebbero essere modificati. Conservare la struttura delle directory usando
"files" equivalente a "data". 
gli script per alterare i database devono stare nei files:

db_varcolan_cmd.sql
db_events_cmd.sql

--------------------
NOTE SULLA VERSIONE
--------------------
questo aggiornamento modifica quanto segue:
- webapp
- application
- database scripts che alterano la struttura del database


--------------------
CREAZIONE PACCHETTO
--------------------
per generare il pacchetto di questa versione:

$ ./genpkg.sh
